# Stupid code

The code in this directory is very stupid, and most of it should never be executed locally 

compile the code using the following command:

```sh
make all
```

execute the files using the following commands:

